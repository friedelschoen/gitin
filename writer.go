package gitin

// #pragma once

// #include "getinfo.h"

// #include <stdio.h>

/* naming: write + <type if not html> + <what is writing> */
/* please order arguments: io.Writer fp, () struct repoinfo* info, int relpath */ // /* please order arguments: io.Writer fp, (const) struct repoinfo* info, int relpath */
/* if a function writes a whole file, it must include writeheader() and writefooter() */
